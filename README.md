TypeScript String `includes` Demo
=================================

Make the code support `includes` method.

The `"target": "es6"` in `tsconfig.json` is the key.

```
npm install
npx ts-node hello.ts
```

It will print `true`